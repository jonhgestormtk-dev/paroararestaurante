rules_version = '2';

// Regras de segurança abertas para desenvolvimento
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}